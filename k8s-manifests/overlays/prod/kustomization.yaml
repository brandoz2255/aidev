apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base resources (now local)
resources:
  - ../../base
  - frontend.yaml
  - nginx.yaml
  - merged-ollama-backend.yaml
  - tts-worker.yaml
  - document-worker.yaml
  - openclaw.yaml

# Namespace
namespace: ai-agents

# Image updates
images:
  - name: harvis-backend
    newName: dulc3/jarvis-backend
    newTag: test10
  - name: harvis-frontend
    newName: dulc3/jarvis-frontend
    newTag: test10
  - name: harvis-document-worker
    newName: dulc3/harvis-document-worker
    newTag: test10
  - name: harvis-tts-worker
    newName: dulc3/harvis-tts-worker   # same layers as jarvis-backend, separate tag for tracking
    newTag: test10
  - name: dulc3/openclaw
    newName: dulc3/openclaw
    newTag: v2.32.2
  - name: dulc3/sglang-patch
    newName: dulc3/sglang-patch
    newTag: test10

# Patches for production
patches:
  - target:
      kind: Deployment
      name: harvis-ai-merged-ollama-backend
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
      - op: replace
        path: /spec/template/spec/containers/1/image
        value: dulc3/jarvis-backend:test10
      - op: replace
        path: /spec/template/spec/initContainers/1/image
        value: dulc3/jarvis-backend:test10
  - target:
      kind: Deployment
      name: harvis-ai-frontend
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: dulc3/jarvis-frontend:test10
  - target:
      kind: Deployment
      name: harvis-document-worker
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: dulc3/harvis-document-worker:test10

  - target:
      kind: Deployment
      name: harvis-ai-tts-worker
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: dulc3/harvis-tts-worker:test10
      - op: replace
        path: /spec/template/spec/initContainers/0/image
        value: dulc3/harvis-tts-worker:test10

  # Inject OpenClaw + Kimi K2.5 env vars into the backend container (index 1 = harvis-backend)
  # so the Python orchestrator knows where to find OpenClaw and has the Moonshot API key.
  - target:
      kind: Deployment
      name: harvis-ai-merged-ollama-backend
    patch: |-
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: OPENCLAW_URL
          value: "ws://harvis-ai-openclaw:18789"
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: OPENCLAW_GATEWAY_TOKEN
          valueFrom:
            secretKeyRef:
              name: harvis-ai-openclaw-secret
              key: openclaw-gateway-token
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: MOONSHOT_API_KEY
          valueFrom:
            secretKeyRef:
              name: harvis-ai-openclaw-secret
              key: moonshot-api-key
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: MOONSHOT_BASE_URL
          value: "https://api.moonshot.ai/v1"
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: NVIDIA_API_KEY
          valueFrom:
            secretKeyRef:
              name: harvis-ai-openclaw-secret
              key: nvidia-api-key
              optional: true
      # HARVIS_GITHUB_TOKEN / HARVIS_GITHUB_USER / HARVIS_GITHUB_EMAIL removed —
      # GitHub auth is now handled by the HarvisAI GitHub App (App ID 2995570).
      # The harvis-github skill mints short-lived installation tokens at runtime
      # using GH_APP_ID + GH_INSTALLATION_ID + GH_PRIVATE_KEY in the openclaw pod.
