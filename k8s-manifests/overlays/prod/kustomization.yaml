apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base resources (now local)
resources:
  - frontend.yaml
  - nginx.yaml
  - merged-ollama-backend.yaml
  - document-worker.yaml
  - openclaw.yaml

# Namespace
namespace: ai-agents

# Image updates
images:
  - name: harvis-backend
    newName: dulc3/jarvis-backend
    newTag: v2.29.15
  - name: harvis-frontend
    newName: dulc3/jarvis-frontend
    newTag: v2.29.15
  - name: harvis-document-worker
    newName: dulc3/harvis-document-worker
    newTag: v2.29.15
  - name: dulc3/openclaw
    newName: dulc3/openclaw
    newTag: v2.29.14

# Patches for production
patches:
  - target:
      kind: Deployment
      name: harvis-ai-merged-ollama-backend
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
      - op: replace
        path: /spec/template/spec/containers/1/image
        value: dulc3/jarvis-backend:v2.29.15
      - op: replace
        path: /spec/template/spec/initContainers/1/image
        value: dulc3/jarvis-backend:v2.29.15
  - target:
      kind: Deployment
      name: harvis-ai-frontend
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: dulc3/jarvis-frontend:v2.29.15
  - target:
      kind: Deployment
      name: harvis-document-worker
    patch: |
      - op: replace
        path: /spec/template/spec/containers/0/image
        value: dulc3/harvis-document-worker:v2.29.15

  # Inject OpenClaw + Kimi K2.5 env vars into the backend container (index 1 = harvis-backend)
  # so the Python orchestrator knows where to find OpenClaw and has the Moonshot API key.
  - target:
      kind: Deployment
      name: harvis-ai-merged-ollama-backend
    patch: |-
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: OPENCLAW_URL
          value: "ws://harvis-ai-openclaw:18789"
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: OPENCLAW_GATEWAY_TOKEN
          valueFrom:
            secretKeyRef:
              name: harvis-ai-openclaw-secret
              key: openclaw-gateway-token
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: MOONSHOT_API_KEY
          valueFrom:
            secretKeyRef:
              name: harvis-ai-openclaw-secret
              key: moonshot-api-key
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: MOONSHOT_BASE_URL
          value: "https://api.moonshot.ai/v1"
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: HARVIS_GITHUB_TOKEN
          valueFrom:
            secretKeyRef:
              name: harvis-ai-openclaw-secret
              key: harvis-github-token
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: HARVIS_GITHUB_USER
          valueFrom:
            secretKeyRef:
              name: harvis-ai-openclaw-secret
              key: harvis-github-user
      - op: add
        path: /spec/template/spec/containers/1/env/-
        value:
          name: HARVIS_GITHUB_EMAIL
          valueFrom:
            secretKeyRef:
              name: harvis-ai-openclaw-secret
              key: harvis-github-email
