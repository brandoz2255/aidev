# FluxCD GitOps Toolkit Components
# This file will be generated by: flux install --export > gotk-components.yaml
#
# To install FluxCD, run:
#   curl -s https://fluxcd.io/install.sh | sudo bash
#   flux install --export > k8s-manifests/flux-system/gotk-components.yaml
#   kubectl apply -f k8s-manifests/flux-system/gotk-components.yaml
#
# Or use the quick install command:
#   flux install
#
# This file is a placeholder. The actual components will be generated during installation.
# See README.md for installation instructions.

# PLACEHOLDER: Run `flux install --export` to generate this file
