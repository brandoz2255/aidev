# requirements.txt for the lightweight worker node

# Core web framework
fastapi
uvicorn[standard]
python-multipart

# HTTP requests to communicate with the main backend
requests

# Pydantic for data validation
pydantic

# Google Gemini API client
google-generativeai