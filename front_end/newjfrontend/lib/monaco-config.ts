/**
 * Monaco Editor language configuration stubs.
 * These provide basic setup for syntax highlighting and LSP-like features.
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function configureMonacoLanguages(_monaco: any): void {
  // Monaco already includes built-in language support for most languages.
  // This function can be extended to register custom language configurations.
}

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export function setupLSPFeatures(_monaco: any, _language: string): void {
  // Placeholder for LSP feature setup.
  // Can be extended to register completion providers, hover providers, etc.
}
