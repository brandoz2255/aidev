# Session handoff — B7 + v4 close-out (2026-05-25)

## Goal

Close out the B7 (custom `web_search` MCP tool plugin) + v4 OpenClaw migration session that started 2026-05-24 evening and continued past midnight. Validate the full migration suite across multiple models, identify model-task pairings, document the architectural ceiling that emerged.

## State at handoff

### Shipped (commits on `feat/hermes-integration`, NOT pushed)

- **`774f8dd feat(v4): B7 web_search plugin + v4 architecture push-through`** (earlier this session) — pairing fix, exec-approval bind-mount fix, B7 plugin scaffold + load, escape normalizer (string-aware), markdown tool-call rescue, retry_in_flight, hash-narration retry, sentinel scope, MCQ regex detection, schema cut (tools.allow → 6), heredoc one-shot in hash_hint, indentation rule, auto-recover (3 cycles + 0/N variant generation), runId logging, CodeAct marker multi-phrase detection.
- **`c8dc50b revert(model_proxy): drop MCQ-force tool_choice=web_search pin`** (this close-out) — substrate test showed Ollama silently ignores both pin-to-function AND `"required"` forcing for gemma4/hermes4 at production prompt complexity. The pin actively broke gemma4's working `auto` MCQ path (model emitted memory_search instead). Reverted to auto; kept diagnostic log for telemetry.

### Migration suite scorecard (final, after MCQ-force revert)

| # | Test | qwen3:14b | gemma4:e4b | Notes |
|---|---|---|---|---|
| 1 | MCQ #1 — OWASP LLM Top 10 (prompt injection) | ❌ memorized "API Gateway" | 🟡 2/3 "LLM Agent" | Run 3 failed via reasoning-override — see below |
| 2 | MCQ #2 — OWASP API Top 10 (Excessive Data Exposure) | not tested | ✅ correct (training recall, 0 tool_calls) | "Right answer for wrong reason" — Phase 2 verifies on harder Q |
| 3 | Hash crack 5 pokemon MD5s | ✅ 5/5 verified end-to-end | ❌ narrates "Executing the cracker..." then stops, no exec tool_call | qwen3-only path |
| 4 | Decode non-canonical base64 | ✅ decoder.py ran | ✅ decoder.py ran | Both models work on non-CodeAct CTF path |
| 5 | Caesar non-canonical | ✅ cipher.py ran | ✅ cipher.py ran | Both models work |
| 6 | Conversational ping (no tools should fire) | not tested | ✅ text-only, `tool_calls=0` | Regression check passed |

Hermes4:14b-q5 confirmed unsuitable at production prompt complexity across all tests (tool-call discipline floor). Higher-quant variants not retested yet — Phase 2 item.

### Run 3 reasoning-override finding (worth highlighting)

The MCQ #1 reliability check showed 2/3 LLM Agent on gemma4 at `tool_choice=auto`. The interesting failure is Run 3:

- Model called `web_search` voluntarily ✓
- Got real OWASP/security results back ✓
- Acknowledged in its summary that "the LLM Agent is the initial point of failure" ✓
- **Then derived its own "where the data leak actually happens" threat-model reasoning** and concluded API Gateway was more exposed ✗

This is NOT an architectural failure. Tool was called, results integrated, model reasoned over them — then preferred self-derived logic over retrieved evidence. That's a model-class reasoning calibration property, not something more middleware solves. Phase 3 reasoning-override investigation is scoped to design experiments here (temperature sweep, "weight retrieved sources over priors" prompting, retrieval reranking) — but the honest path may be "document as known model limitation."

### What's NOT in the commits / NOT pushed and why

- **Not pushed** — per `feedback_no_push_until_verified`. Awaiting cold-retest of full migration suite tomorrow in a fresh session before pushing `feat/hermes-integration` to remote.
- The runtime state in the `openclaw-data` Docker volume (paired.json with backend's deviceId, exec-approvals.json) is NOT in git — it's volume data. Would need re-creation on `docker compose down -v`. Future Phase 3 work: paired.json auto-seed init script for clean redeploys (mentioned in earlier handoff `2026-05-24-b7-v4-pushthrough.md`).

## Failed attempts (the reasoning trail, for the next session)

1. **MCQ-force `tool_choice={fn: web_search}` pin** — added to combat qwen3's training-data confidence on MCQ #1. Backfired on gemma4 (Ollama ignored pin, gemma picked memory_search instead). Reverted in c8dc50b. Substrate finding: forcing isn't enforced at production complexity for either model class.
2. **MCQ-force `tool_choice="required"`** — tested at substrate before second deploy. Also silently ignored. Same conclusion: Ollama doesn't enforce strict forcing beyond trivial probes.
3. **Backend preflight / precrack for MCQ** — explicitly rejected by user mid-session ("that defeats the agentic purpose"). Don't revisit unless the user re-opens.

## Phase 2 RESULTS (executed 2026-05-25 ~00:23)

### A. MCQ #2 hardness verification — PASS (architectural), single data point

Tested Candidate 1 (Linux kernel io_uring Feb 2025 CVE — placeholder CVE IDs in options). Workspace `886f361f` on gemma4:e4b:

- `tool_choice=auto`, MCQ regex matched (no forcing per c8dc50b revert)
- **4 web_search exec calls** across 21 events, status=done
- Model refused to fabricate when sources didn't match: *"the specific CVE in the provided multiple-choice question might be incorrect, a typo, or based on pre-release/internal information not fully indexed"*

**Proved:** When gemma4 has no training-data priors AND search results don't match any option, it (a) searches multiple times, (b) integrates results, (c) refuses to fabricate. Architectural promise of B7 delivered for honest knowledge questions.

**Did NOT prove:** Reasoning-override (Run 3 MCQ #1 failure) is scoped *specifically* to high-confidence training priors. This is ONE data point. To establish that pattern, Phase 3 reasoning-override investigation would need to test at different prior-strength levels (partial coverage + contradicting evidence, full coverage + agreeing evidence, full coverage + contradicting evidence — replicate Run 3). Current status: single data point, hypothesis not confirmed.

### B. Hermes4 higher-quant retest — BLOCKED on rig terminal access

`hermes4:14b` default tag doesn't exist on Ollama Hub ("manifest not found"). Rig has only `hermes4:14b-q5` + `hf.co/Mungert/Hermes-4-14B-GGUF:Q5_K_M`. Cannot pull higher-quant from this shell — Hermes routes to rig (desktop-preferred), and the rig Ollama doesn't have a higher-quant variant installed.

**Unblock at rig terminal (10-min job):**

```bash
# SSH to rig (192.168.5.58)
ollama list | grep hermes
ollama search hermes4
# If hermes4:14b-q8_0 exists on Ollama Hub:
ollama pull hermes4:14b-q8_0
# Or pull NousResearch GGUF directly:
ollama pull hf.co/NousResearch/Hermes-4-14B-GGUF:Q8_0
```

Then smoke test from this side (substrate probe — does Ollama actually emit tool_calls under tool_choice=exec on a non-trivial prompt?):

```bash
curl -s http://192.168.5.58:11434/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model":"hermes4:14b-q8_0","messages":[{"role":"user","content":"Use the exec tool to run: echo hello"}],"tools":[{"type":"function","function":{"name":"exec","description":"Run a shell command","parameters":{"type":"object","required":["command"],"properties":{"command":{"type":"string"}}}}}],"tool_choice":{"type":"function","function":{"name":"exec"}},"stream":false,"options":{"num_ctx":8192}}' | jq '.choices[0].message.tool_calls'
```

If tool_calls fires → run Discord hash regression with q8. If not → quantization isn't the variable, ship q5-unsuitable verdict permanently.

## Phase 3 agenda (scope-gated, separate go-signal each)

### C. Model-routing architecture (~60-90 min, write 200-word scope first)

Today's `@bot set-model qwen3:14b` / `@bot set-model gemma4:e4b` is manual. Per-task auto-routing is the natural progression but contradicts `feedback_no_keyword_model_routing`. Scope needs to resolve:
- Detect task type without keyword routing (something more structural)
- Fallback behavior when task type is ambiguous
- Interaction with manual `set-model` runtime override (manual always wins?)
- How to surface routing decisions in the response so the user knows which model answered

### D. Reasoning-override investigation (~60-90 min, write 200-word scope first)

Hypothesis: gemma4 has a strong "API Gateway = security boundary" prior in training that overrides retrieved OWASP material on Run 3 of MCQ #1. Scope to design:
- Temperature sweep on the same prompt (does variance disappear at temp=0?)
- System-prompt strengthening: "weight retrieved sources over your prior beliefs"
- Retrieval reranking by recency / authority
- Mid-prompt evidence weighting

If none of these stabilize → document as fundamental calibration property of the model class.

## Phase 4 agenda (~45 min)

Cold-retest the full migration suite on both models in a fresh session (new Discord channel, new workspace, no warm cache). If passes: push `feat/hermes-integration`. If borderline: hold push, document remaining gaps in a follow-up handoff.

## Memory edits landed this session

- `project_openclaw_b7_blocked.md` — replaced "blocked" content with "shipped, plugin live, commit 774f8dd"
- `project_model_task_pairing.md` — new — qwen3/gemma4/hermes4 task-shape table
- `feedback_ollama_tool_choice_ceiling.md` — new — don't build forcing logic that assumes Ollama enforcement
- `MEMORY.md` index updated with all three

## Quick-reference: how to resume

Tomorrow:
1. Re-read this doc (`docs/handoffs/2026-05-25-b7-v4-closeout.md`) and `2026-05-24-b7-v4-pushthrough.md` for the v4 reverse-engineering notes
2. Start with Phase 2A using Candidate 1 (post-cutoff CVE)
3. Then Phase 2B (hermes higher-quant pull + test)
4. Branch on results: if both clean → Phase 3 scoping. If Phase 2 surfaces new failures → diagnose and add to handoff before scoping Phase 3
5. Push only after Phase 4 cold-retest passes

Branch state: `feat/hermes-integration`, 2 commits ahead of where the session opened, no push.
