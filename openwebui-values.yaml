# Don't deploy Ollama; we're using our own.
ollama:
  enabled: false

# Configuration for OpenWebUI to connect to your existing Ollama
config:
  vars:
    OLLAMA_BASE_URL: "http://ollama-harvis-prod-merged:11434"
