"""Adaptive Workspace method packs — one module per template that seeds its
tool/capability registry over the generic ``workspace_method`` primitives.
Importing a pack registers it (side effect); ``adaptive_space`` imports the
packs it wants."""
