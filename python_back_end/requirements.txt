# Core web framework and server
fastapi
uvicorn[standard]==0.29.0
python-multipart

# For AI/ML models (TTS, STT, Image Captioning)
# Note: Installing PyTorch with CUDA support requires a specific command.
# Visit https://pytorch.org/get-started/locally/ for instructions.
# Example for CUDA 12.1:
# pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
torch
transformers
chatterbox-tts
soundfile

# For image processing and HTTP requests
Pillow
requests

# Dependencies pulled in by the above packages, but good to have explicitly
# sentencepiece is used by many transformer models
sentencepiece