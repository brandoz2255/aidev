# Harvis backend — the full GPU image's dependency set.
#
# This file is now just the sum of the two halves. Install
# requirements-core.txt alone for a backend that boots without torch and does
# speech in the browser or through the `stt` sidecar; add requirements-voice.txt
# when the backend should run Whisper and the TTS engines in-process.
#
# The default Dockerfile still builds both, so this image is unchanged.
-r requirements-core.txt
-r requirements-voice.txt
